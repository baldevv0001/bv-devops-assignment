.DS_Store
.git/
.gitignore
.idea/
.vscode/
*.swp
*.bak
*.tmp
*.orig
*~
*.tgz

# Local kind values. Anchored, so nothing nested is matched.
/ci/

# Do not add a bare "tests/" pattern: it matches at any depth and would drop
# templates/tests/, so `helm test` would do nothing from a packaged chart.
